# Audio demo task

`audio.demo` records, injects one quote, and checks the resulting transcript.
It leaves the result visible. `audio-support.ts` only handles scrolling to
controls on small screens.

See the [setup and run instructions](../README.md).
