{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "tsc -p tasks/tsconfig.json && tsc -p triggers/tsconfig.json && tsc -p sanitizers/tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "24.13.3",
    "typescript": "5.9.3"
  }
}
